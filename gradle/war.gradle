apply plugin: "war"

bootWar {
    mainClassName = "com.umsa.savepdf.SavePdfApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
